{
  "name": "atlas-health-agent",
  "version": "1.0.0",
  "description": "AI agent for personal health intelligence — sleep, training, nutrition",
  "scripts": {
    "dev":    "vite",
    "build":  "vite build",
    "preview":"vite preview",
    "server": "python server.py",
    "start":  "npm run build && npm run server"
  },
  "dependencies": {
    "react":     "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "vite":                 "^5.3.0"
  }
}
